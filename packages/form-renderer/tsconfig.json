{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": "src",
    // For `src/*.test.ts`, which runs under `node --test`. Dev-only; nothing
    // node-typed is imported by the code that ships in the embed bundle.
    "types": ["node"]
  },
  "include": ["src/**/*"]
}
