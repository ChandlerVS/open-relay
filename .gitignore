/target

.idea/

OpenRelay.md

SECURITY_REVIEW.md

node_modules/
**/dist/
.env
.env.local
.turbo/
*.log

.DS_Store

# Claude Code worktrees: real git worktrees of this repo live here, and a bare
# `git add .` stages one as an embedded-repo gitlink. Shared .claude config
# (settings.json, skills, agents) is deliberately still committable.
.claude/worktrees/
